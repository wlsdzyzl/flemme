# train ae or vae
mode: test
model:
  # can be AE, VAE and DDPM
  name: SeM
  # encoder config
  encoder:
    name: SwinU
    image_size: [256, 256]
    in_channel: 1
    out_channel: 1
    patch_channel: 32
    patch_size: 2
    down_channels: [64, 128, 256]
    middle_channels: [512, 512]
    building_block: swin
    normalization: layer
    activation: relu
  ### loss function
  segmentation_losses: 
    - name: Dice
    - name: BCEL
  # some other information related to the model
  # in ddpm, it can be the time steps and so on.
loader:
  dataset: 
    name: ImgSegDataset
    data_suffix: jpg
    data_dir: image
    label_dir: mask
  data_path_list: 
    - path/to/datasets/thyroid/tn3k/fold5/
  batch_size: 16
  num_workers: 8
  shuffle: false
  data_transforms:
    - name: Resize
      ## the value must be list
      size: [256, 256]
    - name: ToTensor
  label_transforms:
    - name: Resize
      ## the value must be list
      size: [256, 256]
    - name: ToTensor
    - name: ToBinaryMask
      threshold: 0.5
model_path: path/to/checkpoint/thyroid/tn3k/SwinU/ckp_best.pth
seg_dir: path/to/results/seg/tn3k/SwinU
evaluation_metrics:
  seg:
    - name: Dice
    - name: ACC
    - name: mIoU